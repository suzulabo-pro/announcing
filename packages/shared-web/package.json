{
  "name": "@announcing/shared-web",
  "private": true,
  "version": "0.0.0",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.build.json",
    "build.watch": "tsc -p tconfig.build.json -w",
    "test": "echo ok"
  },
  "dependencies": {
    "@announcing/shared": "*",
    "@stencil/core": "2.8.1",
    "firebase": "9.1.1"
  }
}
