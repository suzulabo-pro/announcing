{
  "name": "@announcing/shared",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.build.json",
    "build.watch": "tsc -p tconfig.build.json -w",
    "test": "jest",
    "test.watch": "jest --watch"
  },
  "dependencies": {
    "base-x": "3.0.8",
    "tweetnacl": "1.0.3",
    "type-fest": "2.3.4"
  }
}
