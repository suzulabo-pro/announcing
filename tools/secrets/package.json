{
  "name": "secrets",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "check": "ts-node secrets.ts",
    "deploy": "ts-node secrets.ts --deploy",
    "extract": "ts-node secrets.ts --extract"
  },
  "devDependencies": {
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  }
}
